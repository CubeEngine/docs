# ItemductConfig

```yaml
# The maximum amount of pipe-blocks that are allowed in a network
max-pipes: 1000

# The amount of uses an activator has
activator-uses: 1

# The amount of uses a super activator has ; use -1 for infinite uses
super-activator-uses: 10

```
