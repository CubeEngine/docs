# CommandConfiguration

```yaml
# The maximum number of similar commands to offer when more than one command matches a mistyped command.
max-correction-offers: 5

# The maximum number of offers given for a tab completion request (pressing tab).
max-tab-complete-offers: 5

# A List of commands CubeEngine will not try to override
no-override: []

```
